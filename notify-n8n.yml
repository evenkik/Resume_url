name: Notify n8n on GitHub Pages Build

on:
  page_build:
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify n8n webhook
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{\"status\": \"deployed\", \"repo\": \"${{ github.repository }}\", \"commit\": \"${{ github.sha }}\"}" \
            "https://n8n.srv1034010.hstgr.cloud/webhook-test/fcd6e2a2-d660-48b6-ac80-42f755d66c39"
